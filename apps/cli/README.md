# Rekurn CLI

Install globally:

```bash
npm install -g rekurn
```

Or run with npx:

```bash
npx rekurn --help
```

Rekurn is a TypeScript-first versioning and release tool with local commits,
branches, snapshots, verification, deployment hooks, and API sync.
